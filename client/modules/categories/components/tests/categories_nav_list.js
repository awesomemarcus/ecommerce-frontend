const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import CategoriesNavList from '../categories_nav_list';

describe('categories.components.categories_nav_list', () => {
  it('should do something');
});
